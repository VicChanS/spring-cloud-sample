#
# svc-test：svc-test的所有环境通用配置
# svc-test-dev：svc-test的dev(开发环境)配置
# svc-test-prod：svc-test的prod(生产环境)配置
#
server:
  port: 9999

foo: foo version gggggaaadddsdfdsf
democonfigclient:
  message: hello spring io